const PREFIX = 'channel-page';

const ActionsTypes = {
  LOAD_CHANNEL: `${PREFIX}/load-channel`,
  LOAD_MY_CHANNEL: `${PREFIX}/load-my-channel`,
  GET_STREAMING_KEY: `${PREFIX}/get-streaming-key`,
  RESET_STREAMING_KEY: `${PREFIX}/reset-streaming-key`,
};

export { ActionsTypes };
