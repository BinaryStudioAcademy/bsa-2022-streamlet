enum ActionType {
  LOAD_PREFERENCES = 'preferences/load-preferences',
  CHOOSE_ALL_PREFERENCES = 'preferences/choose-all-preferences',
  BIND_PREFERENCES = 'preferences/bind-preferences',
}

export { ActionType };
