enum ActionType {
  PICK_PREFERENCE = 'preferences/pick-preference',
  CLEAR_PREFERENCES = 'preferences/clear-preferences',
  BIND_PREFERENCES = 'preferences/bind-preferences',
}

export { ActionType };
